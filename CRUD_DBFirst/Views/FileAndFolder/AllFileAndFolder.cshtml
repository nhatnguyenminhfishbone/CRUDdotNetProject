@using System.Data
@using CRUD_DBFirst.Models;
@model IEnumerable<tblFileAndFolder>

@{
    ViewData["Title"] = "FileAndFolder";
    int sl = 1;
}
<div class="row">
    <div class="col-lg-12">
        <a class="btn btn-primary mb-2 float-right" asp-area="" asp-controller="FileAndFolder" asp-action="Add">Add FileAndFolder</a>
    </div>
</div>
<div class="table-responsive-sm">

    @if (ViewBag.msg != null)
    {
        <div class="row">
            <div class="col-md-12 text-center  bg-danger">
                <span class="text-light">@ViewBag.msg</span>
                <span class="text-light float-right">x</span>
            </div>
        </div>
    }
    <table class="table table-bordered table-hover">
        <thead>
            <tr class="table-primary">
                <th scope="col">No</th>
                <th scope="col">Name</th>
                <th scope="col">Modified</th>
                <th scope="col">ModifiedBy</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {

                <tr>
                    <th scope="row">@sl.</th>
                    <td>@item.Name</td>
                    <td>@item.Modified</td>
                    <td>@item.ModifiedBy</td>
                    <td>
                        <a asp-action="Details" asp-controller="FileAndFolder" asp-route-id="@item.Id" class="btn btn-primary">View</a>
                        <a asp-action="Edit" asp-controller="FileAndFolder" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                        <a asp-action="AllFileAndFolder" asp-controller="FileAndFolder" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
                sl++;
            }

        </tbody>
    </table>
</div>